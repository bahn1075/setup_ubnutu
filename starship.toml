# Get editor completions based on the config schema
"$schema" = 'https://starship.rs/config-schema.json'

# Inserts a blank line between shell prompts
add_newline = true

[cmd_duration]
format = "took [$duration]($style) "

# Custom format order
#format = '$directory$kubernetes$memory_usage$custom.gpu$all$character'

# Replace the '❯' symbol in the prompt with '➜'
[character] # The name of the module we are configuring is 'character'
success_symbol = '[➜](bold green)' # The 'success_symbol' segment is being set to '➜' with the color 'bold green'

# Disable the package module, hiding it from the prompt completely
[package]
disabled = true

[directory]
truncation_length = 0
truncate_to_repo = false

[docker_context]
format = 'via [🐋 $context](blue bold)'

[memory_usage]
disabled = false
threshold = -1
symbol = '🐑'
style = 'bold bright-green'
format = 'via $symbol[${ram}]($style) '

# GPU 메모리 사용률을 표시하는 커스텀 모듈
# [custom.gpu]
# command = '''rocm-smi --showmeminfo vram --csv 2>/dev/null | tail -n +2 | awk -F, '{used += $3; total += $2} END {if(total > 0) printf "%.0f", (used/total)*100; else print "N/A"}' '''
# when = "command -v rocm-smi"
# format = '[🎮$output%]($style) '
# style = 'bold bright-red'

[kubernetes]
disabled = false
format = '[$symbol$context \([$namespace](yellow)\)]($style) '
style = 'blue'

[git_branch]
symbol = ' '
truncation_length = 20
truncation_symbol = ''

[git_commit]
commit_hash_length = 4
tag_symbol = '🔖 '

[git_state]
format = '[\($state( $progress_current of $progress_total)\)]($style) '

[git_metrics]
disabled = false
added_style = 'bold blue'
format = '[+$added]($added_style)/[-$deleted]($deleted_style) '

[git_status]
conflicted = '🏳'
ahead = '🏎💨'
behind = '😰'
diverged = '😵'
up_to_date = '[✓](green)'
untracked = '🤷'
stashed = '📦'
modified = '📝'
staged = '[++\($count\)](green)'
renamed = '👅'
deleted = '🗑'

# [os.symbols]
# OracleLinux = '⊂⊃'